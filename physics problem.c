#include<stdio.h>
int main()
{
    double v,t,s;
    printf("Enter the value of v: ");
    scanf("%lf",&v);

        printf("Enter the value of t: ");
        scanf("%lf",&t);

        s=2*v*t;

            printf("the distance is:%.2olf",s);



    return 0;
}
